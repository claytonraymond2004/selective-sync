# Server Configuration
PORT=3001
NODE_ENV=development

# Security
# Required: 32-byte encryption key for storing sensitive data (passwords/keys) in the database.
# You can generate one using: openssl rand -hex 32
ENCRYPTION_KEY=

# Optional: Pre-configure Remote SSH Connection
# If set, these will override database configurations for the initial connection.
# Useful for Docker environments or automated deployments.
REMOTE_HOST=
REMOTE_PORT=22
REMOTE_USER=
REMOTE_PASSWORD=
SSH_KEY_PATH=
